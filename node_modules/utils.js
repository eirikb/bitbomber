var s4 = function() {
    return (((1 + Math.random()) * 0x10000) | 0).toString(16).substring(1);
};

exports.guid = function(times) {
    times = times ? times : 1;
    var guid = '';
    for (var i = 0; i < times; i++) {
        guid += s4();
    }
    return guid;
};
